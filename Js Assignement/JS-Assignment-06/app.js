var firstName = "Sara";
var lastName = "Khan";
var city = "Lahore";

var fullText = "Student: " + firstName + " " + lastName + " from " + city;

document.getElementById("output").textContent = fullText;
