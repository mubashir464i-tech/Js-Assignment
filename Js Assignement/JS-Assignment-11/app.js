function checkAdmission() {
    var age = 20;
    var hasID = true;

    if (age >= 18 && hasID) {
        alert("You can enter the event.");
    } else {
        alert("You cannot enter the event.");
    }
}
